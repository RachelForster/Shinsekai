# Launcher support

This directory owns the cross-platform command and path validation helpers used by
the repository's source launchers and installers. The root launch entry points stay
in their established locations for user compatibility; new support scripts live
here beside the launcher module instead of the top-level `scripts/` directory.
