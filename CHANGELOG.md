# Changelog

本文档记录 Shinsekai 的主要用户可见变更。完整提交记录请参阅对应版本的 GitHub Compare 页面。

## [2.3.1] - 2026-07-26

本版本汇总自 `v2.2.0` 以来合入 `main` 的更新。

### 长期记忆

- 完成长时记忆的设置、初始化、检索、写入与导入流程，并在聊天启动阶段展示加载进度。
- 支持从文本和 JSON 对话记录中分块提取记忆，改进重复检测、队列持久化和关闭时收尾。
- 加强本地嵌入模型与运行时依赖检查，拒绝不完整或损坏的模型快照，并改善 Windows 长路径、下载恢复和就绪状态处理。

### React 聊天界面与主题

- React 聊天界面成为默认运行时，并自动迁移旧版原生聊天配置。
- 新增主题管理、可视化主题编辑和实时预览，加入 Neon Night City、Sakura Dream 与 Spiritron Command 等内置主题。
- 支持九宫格背景、独立背景/文本层、沉浸式自动隐藏控件、窗口置顶、历史定位和多项布局细节优化。
- 修复选项与重同步过程中的白屏、消息覆盖、历史恢复和流式对话重复等问题。

### 输入、语音与多模态

- 统一前端连续 ASR 生命周期，支持静默后提交、自然停顿、暂停/恢复和重复 partial 去抖。
- 支持拖放及选择图片/文件附件，在发送前安全暂存，并为无可用视觉引擎时提供可插拔降级方案。
- 支持批量用户输入和对 LLM/TTS/UI 流水线的及时中断。
- 加强模型输出修复与流式对话协调，避免无效 JSON、乱序回填和重复台词污染历史。

### 插件与扩展界面

- 插件市场新增下载量展示与排序，并改进分页体验。
- 插件前端 iframe 自动携带 Bridge 鉴权信息。
- 新增聊天 UI 插槽、顶部工具栏贡献、通用页面覆盖层和插件用户输入通道，使插件能够安全扩展聊天界面。

### 模板、角色与首次使用

- 增强立绘标签、初始立绘选择和共享弹窗体验。
- 模板会跳过已删除角色、保留角色重命名后的选择，并在启动前统一解析最终角色集合。
- 调整首次使用流程、TTS 下载默认值和紧凑导航，降低首次配置成本。

### 运行时、依赖与下载诊断

- 将 PyTorch 版本与安装策略收归宿主管理；插件声明只表示需要 PyTorch，不再决定具体版本。
- 复用匹配的 `torch`、`torchvision`、`torchaudio`，版本或 CPU/CUDA 构建不匹配时仅替换这组包，避免重装无关依赖。
- 按主机能力选择 CUDA wheel；缺少匹配通道时回退 CPU wheel，并按区域选择官方源或国内镜像。
- 日志会直接显示检测区域和实际 Hugging Face、GitHub、PyPI 来源，同时减少正常轮询日志噪声。

### 安全性与可靠性

- 支持经过校验和鉴权的外部媒体读取，以及 Windows 外部聊天历史路径。
- 加强高风险工具确认关联、插件输入边界和外部路径授权，避免请求或资源串线。
- 修复项目根目录迁移、聊天运行时状态轮询、附件路径和后台任务恢复中的多项边界问题。

### 工程与发布

- Git hooks 迁移到 Husky，并扩充聊天、记忆、插件、模板、下载和安全路径的自动化测试。
- 同步桌面端、前端和运行时清单版本至 `2.3.1`。

[2.3.1]: https://github.com/RachelForster/Shinsekai/compare/v2.2.0...v2.3.1
