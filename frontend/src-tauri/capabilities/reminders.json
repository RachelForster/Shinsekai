{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "reminders",
  "description": "Event access for the local reminder panel.",
  "windows": ["reminders"],
  "permissions": ["core:event:default"]
}
